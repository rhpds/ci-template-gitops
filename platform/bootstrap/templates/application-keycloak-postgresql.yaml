{{ if .Values.postgresql.enabled -}}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: keycloak-postgresql
  namespace: openshift-gitops
spec:
  project: default
  source:
    repoURL: {{ .Values.postgresql.git.repoURL }}
    targetRevision: {{ .Values.postgresql.git.targetRevision }}
    path: {{ .Values.postgresql.git.path }}
  destination:
    server: https://kubernetes.default.svc
  syncPolicy:
    automated:
      enabled: true
    syncOptions:
      - CreateNamespace=true
      - SkipDryRunOnMissingResource=true
      - RespectIgnoreDifferences=true
    retry:
      limit: 10
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
{{- end }}
