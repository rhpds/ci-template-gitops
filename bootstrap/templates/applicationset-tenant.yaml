---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: tenants
  namespace: openshift-gitops
spec:
  generators:
  - list:
      name:
        {{- $.Values.tenant.names | toYaml | nindent 8 }}
  template:
    metadata:
      name: tenant-{{- "{{" }} name {{- " }}" }}
      namespace: openshift-gitops
    spec:
      project: default
      source:
        repoURL: https://github.com/rhpds/ci-template-gitops.git
        revision: gitops-sample
        path: tenant/bootstrap/
        helm:
          values: |
            tenantName: '{{- "{{" }} name {{- " }}" }}'
            user:
              {{- .Values.user | toYaml | nindent 14 }}
      destination:
        server: https://kubernetes.default.svc
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
