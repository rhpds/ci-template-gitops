{{ if and .Values.labs.ocpOps.enabled -}}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: lab-ocpops-{{ .Values.tenant.name }}
  namespace: openshift-gitops
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: tenants
  source:
    repoURL: {{ .Values.labs.ocpOps.git.repoURL }}
    targetRevision: {{ .Values.labs.ocpOps.git.targetRevision }}
    path: {{ .Values.labs.ocpOps.git.path }}
    helm:
      values: |
        tenant:
        {{- .Values.tenant | toYaml | nindent 12 }}
        ocpOps:
        {{- .Values.labs.ocpOps | toYaml | nindent 12 }}
  destination:
    server: https://kubernetes.default.svc
  syncPolicy:
    automated:
      enabled: true
    syncOptions:
      - CreateNamespace=true
      - SkipDryRunOnMissingResource=true
      - RespectIgnoreDifferences=true
    retry:
      limit: 10
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
{{- end }}
